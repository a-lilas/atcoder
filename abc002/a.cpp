// ABC002 A-正直者

#include <iostream>
#include <vector>
#include <queue>
#include <string>
#include <map>
#include <ios>
#include <iomanip>
#include <algorithm>
#define REP(i, n) for(int i=0; i<n; i++)
#define FOR(i, s, n) for(int i=s; i<e; i++)

using namespace std;

int main(){
    long long int X, Y;
    cin >> X >> Y;
    cout << max(X, Y) << endl;
}